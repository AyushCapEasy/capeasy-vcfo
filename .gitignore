# --- Dependencies ---
node_modules/
.pnp
.pnp.*

# --- Secrets / environment — NEVER commit real values ---
.env
.env.*
!.env.example
.env.local
.env.local.*
.admin-credentials.local

# --- Real client data — NEVER commit (D-007: this project stays fake/demo-data-only) ---
# Transient real-client inputs (e.g. a real trial balance for a Stage-1 reality-check) live here
# and are classified IN MEMORY only — never persisted to the DB, never copied anywhere trackable.
.client-data.local/

# --- Next.js / build output ---
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts

# --- Vercel ---
.vercel

# --- Logs / tmp / OS ---
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
tmp/
.DS_Store
Thumbs.db
*.pem
